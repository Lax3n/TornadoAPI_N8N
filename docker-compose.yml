version: '3.8'

services:
  n8n:
    build: .
    container_name: n8n-tornado
    ports:
      - "5678:5678"
    volumes:
      - n8n_data:/home/node/.n8n
    environment:
      - N8N_SECURE_COOKIE=false
    restart: unless-stopped

volumes:
  n8n_data:
